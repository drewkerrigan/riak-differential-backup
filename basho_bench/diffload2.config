{mode, max}.

{duration, 60}.

{concurrent, 1}.

{driver, basho_bench_driver_http_raw}.

{key_generator, {partitioned_sequential_int, 101, 100}}.

{value_generator, {fixed_bin, 10}}.

{http_raw_ips, ["127.0.0.1"]}.
{http_raw_port, 8098}.

{operations, [{put, 1}]}.